<def f='include/c++/13/bitset' l='577' ll='580' type='_WordT &amp; std::_Base_bitset&lt;0&gt;::_M_getword(size_t )'/>
<doc f='include/c++/13/bitset' l='571'>// This would normally give access to the data.  The bounds-checking
      // in the bitset class will prevent the user from getting this far,
      // but this must fail if the user calls _Unchecked_set directly.
      // Let&apos;s not penalize zero-length users unless they actually
      // make an unchecked call; all the memory ugliness is therefore
      // localized to this single should-never-get-this-far function.</doc>
