<def f='ceres-solver/internal/ceres/suitesparse.h' l='113' ll='115' type='void ceres::internal::SuiteSparse::Scale(cholmod_dense * scale, int mode, cholmod_sparse * A)'/>
<doc f='ceres-solver/internal/ceres/suitesparse.h' l='107'>// The matrix A is scaled using the matrix whose diagonal is the
  // vector scale. mode describes how scaling is applied. Possible
  // values are CHOLMOD_ROW for row scaling - diag(scale) * A,
  // CHOLMOD_COL for column scaling - A * diag(scale) and CHOLMOD_SYM
  // for symmetric scaling which scales both the rows and the columns
  // - diag(scale) * A * diag(scale).</doc>
