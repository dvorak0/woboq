<dec f='ceres-solver/internal/ceres/gtest/gtest.h' l='4407' type='testing::internal::FilePath testing::internal::FilePath::RemoveFileName() const'/>
<doc f='ceres-solver/internal/ceres/gtest/gtest.h' l='4401'>// RemoveFileName returns the directory path with the filename removed.
  // Example: FilePath(&quot;path/to/file&quot;).RemoveFileName() returns &quot;path/to/&quot;.
  // If the FilePath is &quot;a_file&quot; or &quot;/a_file&quot;, RemoveFileName returns
  // FilePath(&quot;./&quot;) or, on Windows, FilePath(&quot;.\\&quot;). If the filepath does
  // not have a file, like &quot;just/a/dir/&quot;, it returns the FilePath unmodified.
  // On Windows platform, &apos;\&apos; is the path separator, otherwise it is &apos;/&apos;.</doc>
<use f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='4884' u='c' c='_ZN7testing8internal24XmlUnitTestResultPrinter18OnTestIterationEndERKNS_8UnitTestEi'/>
<def f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8334' ll='8343' type='testing::internal::FilePath testing::internal::FilePath::RemoveFileName() const'/>
<use f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8485' u='c' c='_ZNK7testing8internal8FilePath28CreateDirectoriesRecursivelyEv'/>
<doc f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8328'>// RemoveFileName returns the directory path with the filename removed.
// Example: FilePath(&quot;path/to/file&quot;).RemoveFileName() returns &quot;path/to/&quot;.
// If the FilePath is &quot;a_file&quot; or &quot;/a_file&quot;, RemoveFileName returns
// FilePath(&quot;./&quot;) or, on Windows, FilePath(&quot;.\\&quot;). If the filepath does
// not have a file, like &quot;just/a/dir/&quot;, it returns the FilePath unmodified.
// On Windows platform, &apos;\&apos; is the path separator, otherwise it is &apos;/&apos;.</doc>
