<dec f='include/gflags/gflags.h' l='210' type='bool google::GetCommandLineOption(const char * name, std::string * OUTPUT)'/>
<doc f='include/gflags/gflags.h' l='200'>// --------------------------------------------------------------------
// Normally you access commandline flags by just saying &quot;if (FLAGS_foo)&quot;
// or whatever, and set them by calling &quot;FLAGS_foo = bar&quot; (or, more
// commonly, via the DEFINE_foo macro).  But if you need a bit more
// control, we have programmatic ways to get/set the flags as well.
// These programmatic ways to access flags are thread-safe, but direct
// access is only thread-compatible.

// Return true iff the flagname was found.
// OUTPUT is set to the flag&apos;s value, or unchanged if we return false.</doc>
