<dec f='include/glog/logging.h' l='1209' type='void google::LogMessage::Flush()'/>
<doc f='include/glog/logging.h' l='1206'>// Flush a buffered message to the sink set in the constructor.  Always
  // called by the destructor, it may also be called from elsewhere if
  // needed.  Only the first call is actioned; any later ones are ignored.</doc>
