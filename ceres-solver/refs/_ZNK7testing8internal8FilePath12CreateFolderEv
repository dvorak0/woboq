<dec f='ceres-solver/internal/ceres/gtest/gtest.h' l='4425' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='ceres-solver/internal/ceres/gtest/gtest.h' l='4421'>// Create the directory so that path exists. Returns true if successful or
  // if the directory already exists; returns false if unable to create the
  // directory for any reason, including if the parent directory does not
  // exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
<use f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8486' u='c' c='_ZNK7testing8internal8FilePath28CreateDirectoriesRecursivelyEv'/>
<def f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8493' ll='8509' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='ceres-solver/internal/ceres/gmock_gtest_all.cc' l='8489'>// Create the directory so that path exists. Returns true if successful or
// if the directory already exists; returns false if unable to create the
// directory for any reason, including if the parent directory does not
// exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
