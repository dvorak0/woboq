<dec f='include/glog/logging.h' l='1495' type='void google::base::Logger::Write(bool force_flush, time_t timestamp, const char * message, int message_len)'/>
<doc f='include/glog/logging.h' l='1487'>// Writes &quot;message[0,message_len-1]&quot; corresponding to an event that
  // occurred at &quot;timestamp&quot;.  If &quot;force_flush&quot; is true, the log file
  // is flushed immediately.
  //
  // The input message has already been formatted as deemed
  // appropriate by the higher level logging facility.  For example,
  // textual log messages already contain timestamps, and the
  // file:linenumber header.</doc>
