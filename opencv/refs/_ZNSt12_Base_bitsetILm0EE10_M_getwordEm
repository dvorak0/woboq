<def f='include/c++/4.8/bitset' l='556' ll='561' type='_WordT &amp; std::_Base_bitset&lt;0&gt;::_M_getword(size_t )'/>
<doc f='include/c++/4.8/bitset' l='549'>// This would normally give access to the data.  The bounds-checking
      // in the bitset class will prevent the user from getting this far,
      // but (1) it must still return an lvalue to compile, and (2) the
      // user might call _Unchecked_set directly, in which case this /needs/
      // to fail.  Let&apos;s not penalize zero-length users unless they actually
      // make an unchecked call; all the memory ugliness is therefore
      // localized to this single should-never-get-this-far function.</doc>
