<dec f='opencv/modules/ml/include/opencv2/ml/ml.hpp' l='1594' type='float CvGBTrees::predict(const CvMat * sample, const CvMat * missing = 0, CvMat * weakResponses = 0, CvSlice slice = cvSlice(0, 1073741823), int k = -1) const'/>
<doc f='opencv/modules/ml/include/opencv2/ml/ml.hpp' l='1566'>/*
    // Response value prediction.
    // Parallel version (in the case of TBB existence)
    //
    // API
    // virtual float predict( const CvMat* sample, const CvMat* missing=0,
             CvMat* weak_responses=0, CvSlice slice = CV_WHOLE_SEQ,
             int k=-1 ) const;

    // INPUT
    // sample         - input sample of the same type as in the training set.
    // missing        - missing values mask. missing=0 if there are no
    //                   missing values in sample vector.
    // weak_responses  - predictions of all of the trees.
    //                   not implemented (!)
    // slice           - part of the ensemble used for prediction.
    //                   slice = CV_WHOLE_SEQ when all trees are used.
    // k               - number of ensemble used.
    //                   k is in {-1,0,1,..,&lt;count of output classes-1&gt;}.
    //                   in the case of classification problem
    //                   &lt;count of output classes-1&gt; ensembles are built.
    //                   If k = -1 ordinary prediction is the result,
    //                   otherwise function gives the prediction of the
    //                   k-th ensemble only.
    // OUTPUT
    // RESULT
    // Predicted value.
    */</doc>
<def f='opencv/modules/ml/src/gbt.cpp' l='971' ll='1018' type='float CvGBTrees::predict(const CvMat * _sample, const CvMat * _missing = 0, CvMat *  = 0, CvSlice slice = cvSlice(0, 1073741823), int k = -1) const'/>
<use f='opencv/modules/ml/src/gbt.cpp' l='1387' u='c' c='_ZNK9CvGBTrees7predictERKN2cv3MatES3_RKNS0_5RangeEi'/>
